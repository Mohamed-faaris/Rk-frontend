{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter web build",
  "outputDirectory": "web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!assets/|uploads/).*)", "destination": "/index.html" }
  ]
}
