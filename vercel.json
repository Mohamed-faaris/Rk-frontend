{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter web build",
  "outputDirectory": "web/dist",
  "framework": "vite",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" },
    { "src": "/((?!assets/|uploads/).*)", "dest": "/index.html" }
  ]
}
