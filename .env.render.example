# Render Environment Configuration Template
# Copy this file to .env.render and fill in your values
# Note: Secrets (MONGODB_URI) should be set in Render Dashboard, NOT in code

# ============================================
# APPLICATION ENVIRONMENT
# ============================================

# Environment: production | staging | development
NODE_ENV=production

# Server port (Render uses port 10000 by default)
PORT=10000

# ============================================
# DATABASE CONFIGURATION (MONGODB)
# ============================================

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# For Render: Use MongoDB Atlas connection string
# SET THIS IN RENDER DASHBOARD under "Environment" -> "Add Environment Variable"
# DO NOT commit this to git!
MONGODB_URI=mongodb+srv://username:password@your-cluster.mongodb.net/rk_database?retryWrites=true&w=majority

# Database name (if using custom database)
DB_NAME=rk_database

# Connection pool size for serverless environment
DB_CONNECTION_POOL_SIZE=10

# ============================================
# API CONFIGURATION
# ============================================

# CORS settings (which domains can access your API)
# For production, specify exact domain: https://yourdomain.com
# For development, can use * or localhost
CORS_ORIGIN=*

# API timeout in milliseconds
API_TIMEOUT=30000

# Rate limiting (if implementing)
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# LOGGING
# ============================================

# Log level: debug | info | warn | error
LOG_LEVEL=info

# ============================================
# ADMIN SETTINGS
# ============================================

# Default admin email
ADMIN_EMAIL=admin@example.com

# JWT Secret (for authentication)
# Set this in Render Dashboard under Secrets
# DO NOT commit to git!
JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# ============================================
# RENDER-SPECIFIC SETTINGS
# ============================================

# Render environment name
RENDER_ENV=production

# Build command
BUILD_COMMAND=npm install

# Start command
START_COMMAND=npm start

# ============================================
# THIRD-PARTY API KEYS (if needed)
# ============================================

# Example: Email service API key
# SET THESE IN RENDER DASHBOARD UNDER "SECRETS"
# DO NOT commit to git!
SENDGRID_API_KEY=
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# ============================================
# DEBUG FLAGS
# ============================================

# Enable detailed logging
DEBUG=false

# Enable API request/response logging
LOG_REQUESTS=false
